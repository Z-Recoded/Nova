# browser_hands.adapters
# Per-site/task adapters, each implementing discover(page) -> DiscoveryReport
# and run(page, config) -> RunResult. Empty until M2+ (see CLAUDE.md Phase
# roadmap) — this package is a placeholder created by the M1 harness task.
